# Batch — <краткая цель>

## Tickets

- `<Task>` · `<TicketId>` · `<Role>` — `<назначение>`.

## Dependencies

- `<зависимость или none>`.

## Parallel

- `<какие TicketId разрешено выполнять параллельно>`.

## Completion

- `<условие завершения batch>`.
