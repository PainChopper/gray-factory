# Состояние работ

## Текущее исполнение

- `<Task>` · `<TicketId>` · `<Role>` · `<TicketState>` — `<краткая цель тикета>`.

## История

- `<Task>` · `<TicketId>` · `<Role>` · `<TicketState> (<RoleResult>)` — `<краткий фактический итог>`.
